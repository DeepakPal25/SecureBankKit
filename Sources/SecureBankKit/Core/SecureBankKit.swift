/// SecureBankKit — A reusable security toolkit for banking-style iOS apps.
///
/// This enum serves as the namespace and public entry point for the library.
public enum SecureBankKit {

    /// The current version of SecureBankKit.
    public static let version = "1.0.0"
}
